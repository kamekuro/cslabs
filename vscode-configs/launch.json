{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Project",
            "type": "dotnet",
            "request": "launch",
            "projectPath": "${workspaceFolder}/<PROJECT_NAME>.csproj"
        }
    ]
}